(function(){
  'use strict';
  var tree = require('./walker');
  var dir = process.cwd();
  var output = '';

  var parseElement = function(element) {

  };
  tree(dir, function(err, result){

  });

})();
